/*window.onload = (event) => {
    const booksData = JSON.parse('./books');
    console.log(booksData);
    localStorage.setItem('myData', JSON.stringify('../utils/books.json'))

    document.getElementById('submit').addEventListener('submit', (e) => {
        e.preventDefault;
        const datas = localStorage.getItem('myData');
        console.log(datas);
    }
    )
}*/